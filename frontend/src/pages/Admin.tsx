import { useApi } from '../lib/hooks'
import { PageHeader, Panel, StatusBadge, Loading } from '../components/UI'
import { BellRing, Database, LockKeyhole, Save, SlidersHorizontal, Users } from 'lucide-react'

export function Configuration(){const users=useApi<any[]>('/users',[]);if(users.loading)return <Loading/>;return <><PageHeader eyebrow="Platform administration" title="Configuration" description="Organization roles, review profiles, analyzer policy, AI settings, notifications, and retention controls." actions={<button className="primary-button"><Save size={17}/>Save settings</button>}/><div className="two-col"><Panel title="Review defaults" subtitle="Organization-wide baseline"><div className="form-grid single"><label>Default review profile<select><option>Standard Application</option><option>Security-Sensitive Application</option><option>API Service</option><option>Data / ML Project</option><option>Laravel Application</option></select></label><label>Default minimum severity<select><option>Medium</option><option>High</option><option>Low</option></select></label><label>Retention period<input type="number" defaultValue={180}/><span>days</span></label></div><div className="toggle-list"><label><span>Require approval before publication</span><input type="checkbox" defaultChecked/><i/></label><label><span>Re-review after synchronization</span><input type="checkbox" defaultChecked/><i/></label><label><span>Allow automatic summary publication</span><input type="checkbox"/><i/></label></div></Panel><Panel title="AI reliability controls" subtitle="Structured output and confidence policy"><div className="settings-stack"><div><SlidersHorizontal/><span><strong>Minimum confidence</strong>65% — lower-confidence findings remain hidden</span></div><div><LockKeyhole/><span><strong>Grounding</strong>Require changed file, line, evidence, and suggested resolution</span></div><div><Database/><span><strong>Provider</strong>Mock structured provider in demo; OpenAI-compatible endpoint in live mode</span></div><div><BellRing/><span><strong>Notifications</strong>High-risk, approval-ready, failure, and re-review alerts</span></div></div></Panel></div><Panel title="Organization users" subtitle="Five-role RBAC model"><div className="table-wrap"><table><thead><tr><th>User</th><th>Email</th><th>Role</th><th>Status</th><th>Last login</th></tr></thead><tbody>{users.data.map(x=><tr key={x.id}><td><strong>{x.name}</strong></td><td>{x.email}</td><td>{x.role.replaceAll('_',' ')}</td><td><StatusBadge value={x.is_active?'active':'disabled'}/></td><td>{x.last_login?new Date(x.last_login).toLocaleString():'Never'}</td></tr>)}</tbody></table></div></Panel></>}
export function AuditLogs(){const api=useApi<any[]>('/audit-logs',[]);if(api.loading)return <Loading/>;return <><PageHeader eyebrow="Auditability" title="Audit logs" description="Immutable operational history for authentication, policies, review decisions, publications, and retries."/><Panel><div className="table-wrap"><table><thead><tr><th>Timestamp</th><th>Actor</th><th>Event</th><th>Repository</th><th>Pull request</th><th>Result</th><th>Changed value</th></tr></thead><tbody>{api.data.map(x=><tr key={x.id}><td>{new Date(x.created_at).toLocaleString()}</td><td>{x.actor_name}</td><td><code>{x.event_type}</code></td><td>{x.repository_id||'—'}</td><td>{x.pull_request_id||'—'}</td><td><StatusBadge value={x.result}/></td><td><pre className="json-preview">{JSON.stringify(x.new_value||{},null,0)}</pre></td></tr>)}</tbody></table></div></Panel></>}
