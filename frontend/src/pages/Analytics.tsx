import { Activity, BarChart3, CheckCircle2, ShieldAlert, Target, TimerReset } from 'lucide-react'
import { useApi } from '../lib/hooks'
import { ErrorBox, Loading, MetricCard, PageHeader, Panel, SeverityBadge, StatusBadge } from '../components/UI'

function Bars({items}:{items:any[]}){const max=Math.max(...items.map(x=>x.value),1);return <div className="horizontal-bars">{items.map(x=><div key={x.name}><span>{x.name.replaceAll('_',' ')}</span><div className="bar-track"><i style={{width:`${x.value/max*100}%`}}/></div><strong>{x.value}</strong></div>)}</div>}
export function SecurityDashboard(){const api=useApi<any>('/analytics/security',{});if(api.loading)return <Loading/>;if(api.error)return <ErrorBox message={api.error}/>;const x=api.data;return <><PageHeader eyebrow="Security posture" title="Security dashboard" description="Track critical findings, unresolved risk, repository concentration, and evidence-based security categories."/><div className="metrics-grid four"><MetricCard label="Security findings" value={x.total} icon={ShieldAlert} tone="red"/><MetricCard label="Unresolved" value={x.unresolved} icon={Activity} tone="orange"/><MetricCard label="Repositories affected" value={x.by_repository?.length||0} icon={Target} tone="purple"/><MetricCard label="Mean resolution" value="18.4h" icon={TimerReset} tone="blue"/></div><div className="two-col"><Panel title="Findings by severity"><Bars items={x.by_severity||[]}/></Panel><Panel title="Findings by repository"><Bars items={x.by_repository||[]}/></Panel></div><Panel title="Recent security findings"><div className="table-wrap"><table><thead><tr><th>Finding</th><th>Severity</th><th>File</th><th>Source</th><th>Status</th></tr></thead><tbody>{(x.recent||[]).map((f:any)=><tr key={f.id}><td><strong>{f.title}</strong><span className="muted">{f.category}</span></td><td><SeverityBadge value={f.severity}/></td><td><code>{f.file_path}:{f.start_line}</code></td><td>{f.sources.join(', ')}</td><td><StatusBadge value={f.status}/></td></tr>)}</tbody></table></div></Panel></>}
export function QualityAnalytics(){const api=useApi<any>('/analytics/quality',{});if(api.loading)return <Loading/>;const x=api.data;return <><PageHeader eyebrow="Engineering quality" title="Quality analytics" description="Measure accepted findings, false positives, recurring categories, and human review outcomes."/><div className="metrics-grid four"><MetricCard label="Total findings" value={x.findings} icon={BarChart3} tone="blue"/><MetricCard label="Accepted" value={x.accepted} helper={`${x.accepted_rate}% accepted rate`} icon={CheckCircle2} tone="green"/><MetricCard label="Rejected / suppressed" value={x.rejected} icon={Target} tone="purple"/><MetricCard label="False-positive rate" value={`${x.false_positive_rate}%`} icon={Activity} tone="orange"/></div><div className="two-col"><Panel title="Finding categories"><Bars items={x.by_category||[]}/></Panel><Panel title="Finding lifecycle"><Bars items={x.by_status||[]}/></Panel></div><Panel title="Operational interpretation"><div className="insight-grid"><div><strong>Repository-specific suppression</strong><p>Repeated false positives can become repository-scoped rules without silently changing the model.</p></div><div><strong>Human acceptance</strong><p>Accepted and edited findings measure whether the output remains actionable for reviewers.</p></div><div><strong>Resolution tracking</strong><p>Re-analysis fingerprints map old findings to fixed, still present, or outdated states.</p></div></div></Panel></>}
